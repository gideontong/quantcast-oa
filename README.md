# Quantcast Cookie Parser

My solution to the Quantcast question.

## Getting Started

Use the software:

```sh
python solution.py -f cookie_log.csv -d YYYY-MM-DD
```

Lint the code:

```sh
ruff format
```
